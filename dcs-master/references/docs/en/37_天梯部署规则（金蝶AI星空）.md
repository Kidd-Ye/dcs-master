NO QUERY SPECIFIED. EXAMPLE REQUEST: GET?Q=HELLO&LANGPAIR=EN|IT
---
![image](https://vip.kingdee.com/download/010965590d297bbe409ba43471c86b6d1104.png)
1. Before applying for updating the Synergy 2 patch, please confirm that the project and tenant association application has been submitted to the [Collaborative Development Cloud Platform] (/tolink? target = https %3A % 2F % 2Fdcs.kdgalaxy.com % 2F), and the enterprise administrator has been approved by the ladder portal before you can select the Synergy Artifact Library patch package for the Synergy 2 patch update.

II. If the deployment of the second patch in the current environment is not completed, the bill of lading cannot be submitted.

3. After the second package is opened and deployed, the corresponding environment needs to be restarted before the second package is opened.

4. Sandbox environment update time:

The plan execution time is from 8:00 to 11:55, and the system will deploy the second package to the sandbox environment at 12:00 and automatically restart;

The plan execution time is from 11:55 to 17:55, and the system will deploy the second package to the sandbox environment at 18:00 and automatically restart;

The plan execution time is 17:55 ~ 21:00, the system will deploy the second package to the sandbox environment at 21:00 and automatically restart;

The plan execution time is from 21:00 on the first day to 8:00 on the second day. The system will deploy the second package to the sandbox environment within 30 minutes and automatically restart;

V. Production environment update time:
If the user has a sandbox environment, after the deployment of the sandbox environment is completed, the user clicks [Apply for Deployment to Production] (this button is in the operation column of the operation and maintenance task list), the effective time of different application package types is as follows:

Metadata type, immediately deploy the production environment, effective in real time;

jar type, immediately deploy the production environment, uniformly effective at 5 am;

Metadata jar type, uniformly deploy the production environment at 5 am, and the specific results in real time are subject to the task status;

If the user does not have a sandbox environment, after the user's bill of lading, the system will deploy the second package to the production environment at 5 am the next day and automatically restart, and the specific results will be subject to the task status;

6. The metadata patch package is not subject to the above time window and is effective immediately according to the planned time.