# DCS Master v1.0.5

## 更新摘要

这个版本是一个小版本整理，重点修正 `SKILL.md` 元数据中的 `description` 文案。

## 本次改动

- 精简 `dcs-master/SKILL.md` 的 front matter `description`
- 去掉不必要的品牌名和旧 skill 偏好描述
- 保留核心触发范围，减少触发文案噪音

## 版本说明

- `skill-manifest.json` 已更新到 `1.0.5`
- `README.md` 当前版本号已同步到 `v1.0.5`

## 升级建议

如果你依赖 `SKILL.md` 的元数据触发行为，建议升级到 `v1.0.5`，以使用更干净的触发描述。
